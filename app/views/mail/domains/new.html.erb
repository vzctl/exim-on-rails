<% @title = t('domains.add') %>
<% content_for :content_header_links do %>
  <%= separate_links(t('domains.list') => mail_domains_path) %>
<% end %>
<% form_for :domain, @domain, :url => {:action => :create}, :html => { :method => 'post'} do |f| %>
  <%= render :partial => 'form', :locals => { :f => f, :submit =>t('common.forms.create') } %>
<% end %>
